import { DuelScreen } from "@/components/duel/DuelScreen";

export const metadata = {
  title: "Duelo Vocacional — Teste Vocacional UTFPR",
};

export default function DuelPage() {
  return <DuelScreen />;
}
